# Core RL stack — pinned to match Architecture A repo.
# Keep this list lean: only what the A+γ code actually imports.
myosuite==2.11.6
stable-baselines3==2.7.1
gymnasium==0.29.1
mujoco==3.3.0
dm_control==1.0.28

# Logging / utils — let pip resolve (Python 3.10 / 3.11 compatible)
tensorboard
scipy
matplotlib
pandas
